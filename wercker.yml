box: wercker/default
build:
  steps:
    -  script:
        name: test
        code: |-
          /bin/bash ./run_test.sh
  after-steps:
    - linyows/slack_notification:
        token: $SLACK_TOKEN
        channel: C038M2LE1
